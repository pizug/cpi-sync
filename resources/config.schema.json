{
  "$schema": "http://json-schema.org/draft-07/schema",
  "$id": "http://pizug.com/tool/cpi-sync",
  "type": "object",
  "title": "cpisync config schema",
  "definitions": {
    "credential_s_user": {
      "type": "object",
      "required": ["username"],
      "properties": {
        "username": {
          "type": "string"
        },
        "password_environment_variable": {
          "type": "string"
        }
      },
      "additionalProperties": false
    },
    "credential_oauth_client_credentials": {
      "type": "object",
      "required": ["client_id", "token_endpoint_url"],
      "properties": {
        "client_id": {
          "type": "string",
          "minLength": 1
        },
        "client_secret_environment_variable": {
          "type": "string",
          "minLength": 1
        },
        "token_endpoint_url": {
          "type": "string",
          "format": "uri",
          "pattern": "^https?://"
        }
      },
      "additionalProperties": false
    },
    "credential": {
      "type": "object",
      "title": "The credential schema",
      "properties": {
        "s_user": {
          "$ref": "#/definitions/credential_s_user"
        },
        "oauth_client_credentials": {
          "$ref": "#/definitions/credential_oauth_client_credentials"
        }
      },
      "oneOf": [
        {
          "required": ["s_user"]
        },
        {
          "required": ["oauth_client_credentials"]
        }
      ],
      "additionalProperties": false
    },
    "package_single": {
      "type": "object",
      "required": ["id", "type"],
      "properties": {
        "type": {
          "type": "string",
          "const": "single"
        },
        "id": {
          "type": "string",
          "minLength": 1
        },
        "operation": {
          "type": "string",
          "enum": ["include", "exclude"]
        }
      },
      "additionalProperties": false
    },
    "package_regex": {
      "type": "object",
      "required": ["pattern", "type"],
      "properties": {
        "operation": {
          "type": "string",
          "enum": ["include", "exclude"]
        },
        "type": {
          "type": "string",
          "minLength": 1,
          "const": "regex"
        },
        "pattern": {
          "type": "string",
          "minLength": 1,
          "format": "regex"
        }
      },
      "additionalProperties": false
    }
  },
  "required": ["cpisync", "tenant", "package_rules"],
  "properties": {
    "cpisync": {
      "type": "string",
      "title": "The CPISync schema version",
      "const": "0.1.0"
    },
    "tenant": {
      "type": "object",
      "title": "The tenant schema",
      "required": ["management_host", "credential"],
      "properties": {
        "management_host": {
          "type": "string",
          "format": "hostname"
        },
        "credential": {
          "$ref": "#/definitions/credential"
        }
      },
      "additionalProperties": false
    },
    "package_rules": {
      "type": "array",
      "additionalItems": false,
      "items": {
        "oneOf": [
          {
            "$ref": "#/definitions/package_single"
          },
          {
            "$ref": "#/definitions/package_regex"
          }
        ]
      }
    }
  },
  "additionalProperties": false
}
